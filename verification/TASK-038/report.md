# TASK-038 验证报告

- 状态：`Done`
- 验证日期：2026-09-09
- 分支：`codex/task/TASK-038-real-experiments`

## 已验证能力

- **模式边界**：固定 Demo 会话写入 `demo` 模式，可在讲解时快速推进；普通会话写入 `real` 模式。
- **真实日期锁**：真实模式按 `nextAvailableOn` 判断当天能否记录。完成 9 月 1 日的第 1 天后，第 2 天在 9 月 2 日前无法提交；刷新页面和复制链接不会改变该状态。
- **真实记录闭环**：当天完成、跳过、改期、提前结束都会保存行动、文字、证据类型、感受与下一步。提前结束不会生成“七天结论”。
- **结果与数据控制**：完成实验会显示初始假设与最终反馈的对照；普通会话可以导出本次实验 JSON，或删除该会话的本地数据。
- **兼容迁移**：旧会话若没有模式字段，会按固定 Demo ID 或普通会话分别迁移，避免把旧 Demo 错当作真实跟踪。

## 自动检查

| 检查                | 结果                           |
| ------------------- | ------------------------------ |
| `npm run lint`      | 通过                           |
| `npm run typecheck` | 通过                           |
| `npm test`          | 通过，25 个测试文件、89 项测试 |
| `npm run build`     | 通过                           |

## 浏览器验证

`npm run test:e2e` 已于 2026-09-09 的 TASK-039 回归中补跑，8/8 通过。专项状态机和组件测试也覆盖了真实模式的日期锁、跳过/改期/提前结束、导出，以及 Demo 快速推进不回归。
