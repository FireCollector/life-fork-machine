# TASK-037 验证报告

- 结果：PASS
- 场景：继续工作，还是辞职准备读研？
- 稳定演示：`/demo?scenario=graduate-school`

## 交付内容

1. 新增 `graduate-school` 场景包。它包含“把工作做深 / 辞职集中备考 / 在职验证再决定”三条路线，各三幕、各三种行动。
2. 场景有独立的社区经验卡、项目匹配假设、状态变化、未来反思与七天核验；没有复用创业场景的股权、客户或现金流叙事。
3. 首页可以选择两种正式体验，且仍保留“尝试其他议题”入口。
4. `/play/[sessionId]` 和 `/result/[sessionId]` 在浏览器读取本地会话后，以其中的 `scenarioId` 选择审核内容包；不会根据 URL 或标题猜测。
5. 两个固定 Demo 都能快速推进七天模拟；非 Demo 会话仍必须手动开始和逐日记录。

## 人工来源审核边界

六张来源卡均保留知乎原文链接、条件、后果与审核注释。人工阅读时只抽取可转成核验动作的条件，例如：目标岗位是否把学历视为硬门槛、项目的实践连接、收入中断和共同承担者。

没有把这些内容写成招生政策、收入统计或个人结果预测。原始作者显示名不可见时卡片明确标为“知乎用户”；当年报考资格和项目规则仍须由用户查看官方简章。

## 自动验证

- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npm test`：24 个测试文件、86 项测试通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过（8 项，含新增读研 Demo 全流程）。

浏览器测试从读研固定入口完整走过：查三个项目的硬门槛 → 写转向说明 → 以“暂时不清楚”检验学位假设 → 提交申请并保留工作选项 → 生成报告 → 快速完成七天核验。
