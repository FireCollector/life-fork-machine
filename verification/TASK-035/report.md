# TASK-035 验证报告

- 结果：PASS
- 动态性：候选路线、幕和动作被写入快照键。切换其中任何一项都会换一段候选叙事和“下一句最该问”；相同上下文则保持同一键和同一规则表达。
- 三类线索：每次结果固定显示“支持线索、冲突线索、暂时不清楚”。它们均保留本次检索来源 ID 和原文回链；文案明确说明线索不是对用户结果的预测。
- AI 边界：`/api/narrative` 复用候选专用 AI 合同（`explain-candidate`）。模型只影响候选表达，不能修改正式会话、预算、审核或发布状态。引用不属于当前剧本来源、跨剧本、跨世界动作和敏感文本均会被拒绝。
- 失败降级：AI 失败时接口生成 `rules-assisted` 审核模板，使用同一批来源，且在界面显示清楚的状态标签和原因；已有快照不会被覆盖。
- 恢复：页面把合法快照存入 `life-fork-machine:narrative-snapshots:v1`。同一上下文优先读取保存结果，不重复请求，刷新后可继续使用相同的候选上下文。
- 自动检查：`npm run lint`、`npm run typecheck`、`npm test`（22 个文件、80 项）和 `npm run build` 均通过。
- 人工链路：已确认简报和检索按钮在本地演示页可操作；本次人工走查的知乎实时检索返回 `unavailable`（没有用旧内容伪装成功），因此候选叙事面板以自动化合同、规则降级和生产构建验收为准。
